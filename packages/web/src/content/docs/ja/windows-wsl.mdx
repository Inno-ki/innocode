---
title: Windows
description: Windows install is not supported yet.
---

InnoCode does not support native Windows installs yet. For now, use one of the supported options:

- Install on macOS or Linux.
- Use Docker: `docker run -it --rm ghcr.io/inno-ki/innocode`.

When Windows support is available, this page will be updated.
