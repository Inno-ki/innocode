---
title: CLI
description: أوامر وخيارات CLI في InnoCode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

يبدأ CLI الخاص بـ InnoCode افتراضيا واجهة [TUI](/docs/tui) عند تشغيله بدون أي معاملات.

```bash
innocode
```

كما يدعم أيضا أوامر موثقة في هذه الصفحة، ما يتيح لك التفاعل مع InnoCode برمجيا.

```bash
innocode run "Explain how closures work in JavaScript"
```

---

### tui

بدء واجهة المستخدم terminal (TUI) الخاصة بـ InnoCode.

```bash
innocode [project]
```

#### الخيارات

| الخيار       | المختصر | الوصف                                                             |
| ------------ | ------- | ----------------------------------------------------------------- |
| `--continue` | `-c`    | متابعة الجلسة الأخيرة                                             |
| `--session`  | `-s`    | معرّف الجلسة للمتابعة                                             |
| `--fork`     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`) |
| `--prompt`   |         | الموجّه المراد استخدامه                                           |
| `--model`    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                      |
| `--agent`    |         | الوكيل المراد استخدامه                                            |
| `--port`     |         | المنفذ الذي يتم الاستماع عليه                                     |
| `--hostname` |         | اسم المضيف الذي يتم الاستماع عليه                                 |

---

## الأوامر

يتضمن CLI الخاص بـ InnoCode أيضا الأوامر التالية.

---

### agent

إدارة وكلاء InnoCode.

```bash
innocode agent [command]
```

---

### attach

إرفاق terminal بخادم الواجهة الخلفية لـ InnoCode قيد التشغيل بالفعل، والذي تم تشغيله عبر الأمرين `serve` أو `web`.

```bash
innocode attach [url]
```

يتيح ذلك استخدام واجهة TUI مع واجهة خلفية لـ InnoCode تعمل عن بعد. على سبيل المثال:

```bash
# ابدأ خادم الواجهة الخلفية للوصول عبر الويب/الجوال
innocode web --port 4096 --hostname 0.0.0.0

# في محطة طرفية (terminal) أخرى، اربط TUI بالواجهة الخلفية قيد التشغيل
innocode attach http://10.20.30.40:4096
```

#### الرايات

| الراية      | المختصر | الوصف                               |
| ----------- | ------- | ----------------------------------- |
| `--dir`     |         | دليل العمل الذي ستبدأ منه واجهة TUI |
| `--session` | `-s`    | معرّف الجلسة للمتابعة               |

---

#### create

إنشاء وكيل جديد بإعدادات مخصصة.

```bash
innocode agent create
```

سيرشدك هذا الأمر خلال إنشاء وكيل جديد مع موجه نظام مخصص وإعدادات الأدوات.

---

#### list

عرض جميع الوكلاء المتاحين.

```bash
innocode agent list
```

---

### auth

أمر لإدارة بيانات الاعتماد وتسجيل الدخول لمزودي الخدمة.

```bash
innocode auth [command]
```

---

#### login

يعتمد InnoCode على قائمة المزودين في [Models.dev](https://models.dev)، لذا يمكنك استخدام `innocode auth login` لتهيئة مفاتيح API لأي مزود ترغب باستخدامه. يتم حفظ ذلك في `~/.local/share/innocode/auth.json`.

```bash
innocode auth login
```

عند تشغيل InnoCode يقوم بتحميل المزودين من ملف بيانات الاعتماد، وكذلك أي مفاتيح معرّفة في متغيرات البيئة لديك أو في ملف `.env` ضمن مشروعك.

---

#### list

يسرد جميع المزودين الذين تم توثيقهم كما هم محفوظون في ملف بيانات الاعتماد.

```bash
innocode auth list
```

أو النسخة المختصرة.

```bash
innocode auth ls
```

---

#### logout

يسجلك خارج مزود عبر حذفه من ملف بيانات الاعتماد.

```bash
innocode auth logout
```

---

### github

إدارة وكيل GitHub لأتمتة المستودع.

```bash
innocode github [command]
```

---

#### install

تثبيت وكيل GitHub في مستودعك.

```bash
innocode github install
```

يقوم ذلك بإعداد سير عمل GitHub Actions اللازم ويرشدك خلال عملية التهيئة. [اعرف المزيد](/docs/github).

---

#### run

تشغيل وكيل GitHub. يُستخدم هذا عادة ضمن GitHub Actions.

```bash
innocode github run
```

##### الرايات

| الراية    | الوصف                                |
| --------- | ------------------------------------ |
| `--event` | حدث GitHub مُحاكى لتشغيل الوكيل عليه |
| `--token` | رمز وصول شخصي لـ GitHub              |

---

### mcp

إدارة خوادم Model Context Protocol.

```bash
innocode mcp [command]
```

---

#### add

إضافة خادم MCP إلى تهيئتك.

```bash
innocode mcp add
```

سيرشدك هذا الأمر خلال إضافة خادم MCP محلي أو بعيد.

---

#### list

عرض جميع خوادم MCP المُهيأة وحالة اتصالها.

```bash
innocode mcp list
```

أو استخدم النسخة المختصرة.

```bash
innocode mcp ls
```

---

#### auth

إجراء المصادقة مع خادم MCP يدعم OAuth.

```bash
innocode mcp auth [name]
```

إذا لم تُحدِّد اسم خادم، فسيُطلب منك الاختيار من الخوادم المتاحة الداعمة لـ OAuth.

يمكنك أيضا عرض الخوادم الداعمة لـ OAuth وحالة المصادقة الخاصة بها.

```bash
innocode mcp auth list
```

أو استخدم النسخة المختصرة.

```bash
innocode mcp auth ls
```

---

#### logout

إزالة بيانات اعتماد OAuth لخادم MCP.

```bash
innocode mcp logout [name]
```

---

#### debug

تشخيص مشاكل اتصال OAuth لخادم MCP.

```bash
innocode mcp debug <name>
```

---

### models

عرض جميع النماذج المتاحة من المزودين المُهيأين.

```bash
innocode models [provider]
```

يعرض هذا الأمر جميع النماذج المتاحة عبر المزودين المُهيأين لديك بصيغة `provider/model`.

هذا مفيد لتحديد اسم النموذج الدقيق الذي ستستخدمه في [تهيئتك](/docs/config/).

يمكنك اختياريا تمرير معرّف مزود لتصفية النماذج حسب ذلك المزود.

```bash
innocode models anthropic
```

#### الرايات

| الراية      | الوصف                                                         |
| ----------- | ------------------------------------------------------------- |
| `--refresh` | تحديث ذاكرة التخزين المؤقت للنماذج من models.dev              |
| `--verbose` | استخدام مخرجات أكثر تفصيلا للنماذج (تشمل بيانات مثل التكاليف) |

استخدم الراية `--refresh` لتحديث قائمة النماذج المخزنة مؤقتا. يفيد ذلك عند إضافة نماذج جديدة إلى مزود وتريد رؤيتها في InnoCode.

```bash
innocode models --refresh
```

---

### run

تشغيل innocode في وضع غير تفاعلي عبر تمرير موجه مباشرة.

```bash
innocode run [message..]
```

هذا مفيد للبرمجة النصية والأتمتة، أو عندما تريد إجابة سريعة دون تشغيل واجهة TUI كاملة. على سبيل المثال.

```bash "innocode run"
innocode run Explain the use of context in Go
```

يمكنك أيضا الإرفاق بمثيل `innocode serve` قيد التشغيل لتجنّب زمن الإقلاع البارد لخوادم MCP في كل تشغيل:

```bash
# ابدأ خادمًا بلا واجهة في محطة طرفية واحدة
innocode serve

# في محطة طرفية أخرى، شغّل الأوامر التي ترتبط به
innocode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### الرايات

| الراية       | المختصر | الوصف                                                             |
| ------------ | ------- | ----------------------------------------------------------------- |
| `--command`  |         | الأمر المراد تشغيله؛ استخدم الرسالة كوسائط                        |
| `--continue` | `-c`    | متابعة الجلسة الأخيرة                                             |
| `--session`  | `-s`    | معرّف الجلسة للمتابعة                                             |
| `--fork`     |         | تفريع الجلسة عند المتابعة (يستخدم مع `--continue` أو `--session`) |
| `--share`    |         | مشاركة الجلسة                                                     |
| `--model`    | `-m`    | النموذج المراد استخدامه بصيغة provider/model                      |
| `--agent`    |         | الوكيل المراد استخدامه                                            |
| `--file`     | `-f`    | ملف/ملفات لإرفاقها بالرسالة                                       |
| `--format`   |         | التنسيق: default (منسق) أو json (أحداث JSON خام)                  |
| `--title`    |         | عنوان للجلسة (يستخدم موجهًا مقتطعًا إن لم تُحدَّد قيمة)           |
| `--attach`   |         | الإرفاق بخادم innocode قيد التشغيل (مثل http://localhost:4096)    |
| `--port`     |         | منفذ الخادم المحلي (الافتراضي منفذ عشوائي)                        |

---

### serve

بدء خادم InnoCode بدون واجهة للوصول عبر API. راجع [توثيق الخادم](/docs/server) لواجهة HTTP الكاملة.

```bash
innocode serve
```

يشغّل هذا خادم HTTP يوفّر وصولا عبر API لوظائف innocode دون واجهة TUI. اضبط `INNOCODE_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `innocode`).

#### الرايات

| الراية       | الوصف                                     |
| ------------ | ----------------------------------------- |
| `--port`     | المنفذ الذي يتم الاستماع عليه             |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه         |
| `--mdns`     | تفعيل اكتشاف mDNS                         |
| `--cors`     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### session

إدارة جلسات InnoCode.

```bash
innocode session [command]
```

---

#### list

عرض جميع جلسات InnoCode.

```bash
innocode session list
```

##### الرايات

| الراية        | المختصر | الوصف                                 |
| ------------- | ------- | ------------------------------------- |
| `--max-count` | `-n`    | حصر النتائج في أحدث N جلسات           |
| `--format`    |         | تنسيق المخرجات: table أو json (table) |

---

### stats

عرض إحصاءات استخدام الرموز والتكلفة لجلسات InnoCode لديك.

```bash
innocode stats
```

#### الرايات

| الراية      | الوصف                                                                     |
| ----------- | ------------------------------------------------------------------------- |
| `--days`    | عرض الإحصاءات لآخر N يومًا (الافتراضي: كل الوقت)                          |
| `--tools`   | عدد الأدوات المطلوب عرضها (الافتراضي: الكل)                               |
| `--models`  | عرض تفصيل استخدام النماذج (مخفي افتراضيا). مرّر رقمًا لعرض أعلى N         |
| `--project` | التصفية حسب المشروع (الافتراضي: كل المشاريع، سلسلة فارغة: المشروع الحالي) |

---

### export

تصدير بيانات الجلسة بصيغة JSON.

```bash
innocode export [sessionID]
```

إذا لم تُقدّم معرّف جلسة، فسيُطلب منك الاختيار من الجلسات المتاحة.

---

### import

استيراد بيانات الجلسة من ملف JSON أو رابط مشاركة InnoCode.

```bash
innocode import <file>
```

يمكنك الاستيراد من ملف محلي أو من رابط مشاركة InnoCode.

```bash
innocode import session.json
innocode import https://innocode.io/s/abc123
```

---

### web

بدء خادم InnoCode بدون واجهة مع واجهة ويب.

```bash
innocode web
```

يشغّل هذا خادم HTTP ويفتح متصفح ويب للوصول إلى InnoCode عبر واجهة ويب. اضبط `INNOCODE_SERVER_PASSWORD` لتفعيل مصادقة HTTP الأساسية (اسم المستخدم الافتراضي `innocode`).

#### الرايات

| الراية       | الوصف                                     |
| ------------ | ----------------------------------------- |
| `--port`     | المنفذ الذي يتم الاستماع عليه             |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه         |
| `--mdns`     | تفعيل اكتشاف mDNS                         |
| `--cors`     | أصول/منشأات إضافية للمتصفح للسماح بـ CORS |

---

### acp

بدء خادم ACP (Agent Client Protocol).

```bash
innocode acp
```

يشغّل هذا الأمر خادم ACP يتواصل عبر stdin/stdout باستخدام nd-JSON.

#### الرايات

| الراية       | الوصف                             |
| ------------ | --------------------------------- |
| `--cwd`      | دليل العمل                        |
| `--port`     | المنفذ الذي يتم الاستماع عليه     |
| `--hostname` | اسم المضيف الذي يتم الاستماع عليه |

---

### uninstall

إلغاء تثبيت InnoCode وإزالة جميع الملفات ذات الصلة.

```bash
innocode uninstall
```

#### الرايات

| الراية          | المختصر | الوصف                               |
| --------------- | ------- | ----------------------------------- |
| `--keep-config` | `-c`    | الإبقاء على ملفات التهيئة           |
| `--keep-data`   | `-d`    | الإبقاء على بيانات الجلسات واللقطات |
| `--dry-run`     |         | عرض ما سيتم حذفه دون تنفيذ الحذف    |
| `--force`       | `-f`    | تخطي مطالبات التأكيد                |

---

### upgrade

تحديث innocode إلى أحدث إصدار أو إلى إصدار محدد.

```bash
innocode upgrade [target]
```

للترقية إلى أحدث إصدار.

```bash
innocode upgrade
```

للترقية إلى إصدار محدد.

```bash
innocode upgrade v0.1.48
```

#### الرايات

| الراية     | المختصر | الوصف                                                       |
| ---------- | ------- | ----------------------------------------------------------- |
| `--method` | `-m`    | طريقة التثبيت المستخدمة: curl أو npm أو pnpm أو bun أو brew |

---

## الخيارات العامة

يدعم سطر أوامر innocode الخيارات العامة التالية.

| الراية         | المختصر | الوصف                                  |
| -------------- | ------- | -------------------------------------- |
| `--help`       | `-h`    | عرض المساعدة                           |
| `--version`    | `-v`    | طباعة رقم الإصدار                      |
| `--print-logs` |         | طباعة السجلات إلى stderr               |
| `--log-level`  |         | مستوى السجل (DEBUG, INFO, WARN, ERROR) |

---

## متغيرات البيئة

يمكن تهيئة InnoCode باستخدام متغيرات البيئة.

| المتغير                               | النوع   | الوصف                                                     |
| ------------------------------------- | ------- | --------------------------------------------------------- |
| `INNOCODE_AUTO_SHARE`                 | boolean | مشاركة الجلسات تلقائيا                                    |
| `INNOCODE_GIT_BASH_PATH`              | string  | مسار ملف Git Bash التنفيذي على Windows                    |
| `INNOCODE_CONFIG`                     | string  | مسار ملف التهيئة                                          |
| `INNOCODE_CONFIG_DIR`                 | string  | مسار دليل التهيئة                                         |
| `INNOCODE_CONFIG_CONTENT`             | string  | محتوى تهيئة JSON مُضمّن                                   |
| `INNOCODE_DISABLE_AUTOUPDATE`         | boolean | تعطيل التحقق التلقائي من التحديثات                        |
| `INNOCODE_DISABLE_PRUNE`              | boolean | تعطيل تنقية البيانات القديمة                              |
| `INNOCODE_DISABLE_TERMINAL_TITLE`     | boolean | تعطيل تحديث عنوان terminal تلقائيا                        |
| `INNOCODE_PERMISSION`                 | string  | تهيئة أذونات JSON مُضمّنة                                 |
| `INNOCODE_DISABLE_DEFAULT_PLUGINS`    | boolean | تعطيل الإضافات الافتراضية                                 |
| `INNOCODE_DISABLE_LSP_DOWNLOAD`       | boolean | تعطيل تنزيل خوادم LSP تلقائيا                             |
| `INNOCODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | تفعيل النماذج التجريبية                                   |
| `INNOCODE_DISABLE_AUTOCOMPACT`        | boolean | تعطيل ضغط السياق تلقائيا                                  |
| `INNOCODE_DISABLE_CLAUDE_CODE`        | boolean | تعطيل القراءة من `.claude` (الموجه + المهارات)            |
| `INNOCODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | تعطيل قراءة `~/.claude/CLAUDE.md`                         |
| `INNOCODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | تعطيل تحميل `.claude/skills`                              |
| `INNOCODE_DISABLE_MODELS_FETCH`       | boolean | تعطيل جلب النماذج من مصادر بعيدة                          |
| `INNOCODE_FAKE_VCS`                   | string  | مزود VCS وهمي لأغراض الاختبار                             |
| `INNOCODE_DISABLE_FILETIME_CHECK`     | boolean | تعطيل التحقق من وقت الملف لتحسين الأداء                   |
| `INNOCODE_CLIENT`                     | string  | معرّف العميل (الافتراضي `cli`)                            |
| `INNOCODE_ENABLE_EXA`                 | boolean | تفعيل أدوات بحث الويب من Exa                              |
| `INNOCODE_SERVER_PASSWORD`            | string  | تفعيل المصادقة الأساسية لخادمي `serve`/`web`              |
| `INNOCODE_SERVER_USERNAME`            | string  | تجاوز اسم مستخدم المصادقة الأساسية (الافتراضي `innocode`) |
| `INNOCODE_MODELS_URL`                 | string  | رابط مخصص لجلب تهيئة النماذج                              |

---

### تجريبي

تقوم متغيرات البيئة التالية بتفعيل ميزات تجريبية قد تتغير أو تتم إزالتها.

| المتغير                                         | النوع   | الوصف                                       |
| ----------------------------------------------- | ------- | ------------------------------------------- |
| `INNOCODE_EXPERIMENTAL`                         | boolean | تفعيل جميع الميزات التجريبية                |
| `INNOCODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | تفعيل اكتشاف الأيقونات                      |
| `INNOCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | تعطيل النسخ عند التحديد في TUI              |
| `INNOCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | المهلة الافتراضية لأوامر bash بالميلي ثانية |
| `INNOCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | الحد الأقصى لرموز المخرجات لاستجابات LLM    |
| `INNOCODE_EXPERIMENTAL_FILEWATCHER`             | boolean | تفعيل مراقب الملفات لكامل الدليل            |
| `INNOCODE_EXPERIMENTAL_OXFMT`                   | boolean | تفعيل مُنسّق oxfmt                          |
| `INNOCODE_EXPERIMENTAL_LSP_TOOL`                | boolean | تفعيل أداة LSP تجريبية                      |
| `INNOCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | تعطيل مراقب الملفات                         |
| `INNOCODE_EXPERIMENTAL_EXA`                     | boolean | تفعيل ميزات Exa التجريبية                   |
| `INNOCODE_EXPERIMENTAL_LSP_TY`                  | boolean | تفعيل تدقيق أنواع LSP تجريبي                |
| `INNOCODE_EXPERIMENTAL_MARKDOWN`                | boolean | تفعيل ميزات markdown تجريبية                |
| `INNOCODE_EXPERIMENTAL_PLAN_MODE`               | boolean | تفعيل وضع الخطة                             |
